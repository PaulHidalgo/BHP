<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bhp.securitytest">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InitialActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/login" />
        <activity
            android:name=".RegisterActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/register" />
        <activity
            android:name=".presentation.PresentationActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/bhp"
            android:screenOrientation="sensorLandscape" />
        <activity
            android:name=".DashboardActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_dashboard" />
        <activity
            android:name=".VisitActivity"
            android:label="@string/visitors" />
        <activity
            android:name=".OptionActivity"
            android:label="@string/return_menu_principal" />

        <receiver
            android:name=".broadcast.AlarmRegisterExit"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver" />
        <receiver
            android:name=".broadcast.AlarmNotifications"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter android:priority="0">
                <action android:name="com.bhp.securitytest.broadcast.BROADCAST_NOTIFICATION" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.PushReceiverIntentService"
            android:exported="false"></service>
    </application>

</manifest>